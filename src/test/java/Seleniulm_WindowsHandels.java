import java.util.Set;

public class Seleniulm_WindowsHandels {


    public static void main(String [] argus) {

//    WebDriver driver = new ChromeDriver();

//
//        String Mainwindow = driver.getWindowHandel();
//
//        System.out.println("main window handel is ="+Mainwindow);
//
//        Set<String> handles = driver.getWindowHandels();
//
//        for(String handle : handels){
//

//        String windowTitle = driver.switchTo().window(handle).getTitle();
//
//            if(windowTitle.equals(ExpectedwindowTitle)){
//
//                driver.findElements(By.Xpath('//*[@name="nametext"]'));
//
//                driver.close();
//
//
//            }
//            driver.switchTo().window(Mainwindow);




//                System.setProperty("webdriver.chrome.driver", "path/to/chromedriver");
//                WebDriver driver = new ChromeDriver();
//
//                driver.get("https://example.com"); // Open the main website
//
//                String parentWindow = driver.getWindowHandle(); // Get the Parent Window ID
//
//                // Assume there are 100 windows already opened, or you open them using a loop
//                Set<String> allWindows = driver.getWindowHandles(); // Get all window handles
//
//                System.out.println("Total Windows: " + allWindows.size()); // Print total windows
//
//                // Iterate through all windows
//                for (String window : allWindows) {
//                    driver.switchTo().window(window); // Switch to the window
//                    System.out.println("Switched to Window: " + driver.getTitle());
//
//                    // Perform some actions (e.g., print title, close if needed)
//                    if (!window.equals(parentWindow)) {
//                        driver.close(); // Close the current window (optional)
//                    }
//                }
//
//                // Switch back to the Parent Window
//                driver.switchTo().window(parentWindow);
//                System.out.println("Switched back to Parent Window: " + driver.getTitle());
//
//                driver.quit(); // Close all remaining windows and end session


//            }
//        }



    }

    }
